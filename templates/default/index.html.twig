{% extends 'base.html.twig' %}

{% block title %}Hello PublicController!
{% endblock %}

{% block container %}
{# <div class="list-group w-auto">
	{% for todo in [1,2,3,4,5] %}
		{% include "default/_todo_item.html.twig" %}
	{% endfor %}
</div> #}
{# <div class="spinner-loader d-flex justify-content-center align-items-center">
  <div class="spinner-border" role="status">
    <span class="visually-hidden">Loading...</span>
  </div>
</div> #}
	{# <div class="d-flex justify-content-center">
	</div> #}

	<div class="row justify-content-center">
		<a href="{{ path('todos_add') }}" id="add_btn" class="btn btn-primary col-8 my-4">Add ToDo</a>

		<div class="loader col-8 text-center">Loading...</div>
		<div class="list-group list-group-flush col-8"></div>
	</div>
{% endblock %}
